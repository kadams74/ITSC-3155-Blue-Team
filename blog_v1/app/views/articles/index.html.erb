<div id="page-container"> <%# everything within the page will be put in a container for the footer%>
<%= render 'navigation' %>

<h1>Movie Articles</h1>
<%= link_to 'New article', new_article_path %>
<table>
  <tr>
    <div class="dropdown">
  <button class="dropbtn">Sort By</button>
  <div class="dropdown-content">
    <a href=sort_user>UserName</a>
    <a href=sort_movie>Movie</a>
    <a href=sort_user>User</a>
    <a href=sort_date>Date</a>
  </div>
</div>

    <th>Title</th>
    <th>Text</th>
  </tr>
 
  <% @articles.each do |article| %>
    <tr>
      <td><%= article.title %></td>
      <td><%= article.text %></td>
      <td><%= link_to 'Show', article_path(article) %></td>
      <td><%= link_to 'Edit', edit_article_path(article) %></td>
      <td><%= link_to 'Destroy',
                                article_path(article),
                                method: :delete,
                                data: {confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
</table>
<%= render 'footer' %>
</div>